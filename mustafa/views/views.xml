<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="mustafa.list">
      <field name="name">mustafa list</field>
      <field name="model">mustafa.mustafa</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="mustafa.action_window">
      <field name="name">mustafa window</field>
      <field name="res_model">mustafa.mustafa</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="mustafa.action_server">
      <field name="name">mustafa server</field>
      <field name="model_id" ref="model_mustafa_mustafa"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="mustafa" id="mustafa.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="mustafa.menu_1" parent="mustafa.menu_root"/>
    <menuitem name="Menu 2" id="mustafa.menu_2" parent="mustafa.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="mustafa.menu_1_list" parent="mustafa.menu_1"
              action="mustafa.action_window"/>
    <menuitem name="Server to list" id="mustafa" parent="mustafa.menu_2"
              action="mustafa.action_server"/>
    -->
  </data>
</odoo>